{
  "name": "apify-matcher-utils",
  "version": "0.0.24",
  "description": "This version is same the version 0.0.23, but there no log for result and block resource",
  "main": "utils.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "apify",
    "puppeteer",
    "matcher"
  ],
  "author": "egislook",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.2.0"
  }
}
